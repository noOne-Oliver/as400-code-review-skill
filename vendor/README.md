# Vendor Files

This directory is reserved for vendored tooling needed to validate the skill when system-level dependencies are unavailable.

The repository now vendors `quick_validate.py` here so local validation can run without depending on a machine-specific Codex install.

The expected path is:

```text
vendor/skill-creator/quick_validate.py
```
